#include <stdio.h>

/**
 * main - Entry point
 *
 *decription: prints to the console
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char[] = "with proper grammar, but the outcome is a piece of art,";
	
	printf("%c \n" char);

	return (0);
}
