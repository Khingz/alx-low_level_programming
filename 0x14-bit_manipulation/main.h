#ifndef MAIN_H
#define MAIN_H

/* header files */
#include <stddef.h>
#include <string.h>

/* function prototype */
unsigned int binary_to_uint(const char *b);
void print_binary(unsigned long int n);
int _putchar(char c);

#endif
