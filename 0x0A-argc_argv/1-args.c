#include <stdio.h>

/**
 * main - Entry point
 *
 * @argc: argument count
 * @argv: argument vector
 *
 * Return: always zero
 */

int main(int argc, char __attribute__((__unused__)) *argv[])
{
	printf("%i\n", (argc - 1));
	return (0);
}
